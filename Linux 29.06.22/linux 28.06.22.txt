ssh (протокол для подключения к серверу) ssh xsp@mongodb.ab88.spac 
tail (команда для вывода последних 4x строк в запросе) tail -4
grep (команда для поиска ключевого текста в файле) grep “world” file.txt
find (команда для поиска файла в каталоге c именем начинающимся на “fi” ) find . -type f -name "fi*”
file (команда для отображения типов файлов в каталоге) file *
cat (команда для просмотра содержимого файла) cat file.txt
mkdir (команда для создания нового каталога c созданием полного дерева) mkdir -p  /home/user/dir1/dir2